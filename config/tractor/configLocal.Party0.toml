## Shared keys
shared_keys_path = "example_data/keys"

## Data files: none for party 0

## Output and cache paths
output_dir = "out/party0_tractor"
cache_dir = "cache/party0_tractor"

## Computational parameters
local_num_threads = 4
memory_limit = 4000000000
